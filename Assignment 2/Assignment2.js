let names = ["Letsupgarde", "Me", "You", "Him", "Her"]
console.log(names);
for (i=names.length; i>=0; i--)
{
    console.log(names[i]);
}

console.log("")

for (x=1; x<=50; x++)
{
    if (x%5 == 0)
    {
        console.log(x);
    }
}